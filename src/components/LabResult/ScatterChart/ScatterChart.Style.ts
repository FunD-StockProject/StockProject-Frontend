import styled from '@emotion/styled';

export const ChartContainer = styled.div`
  width: 100%;
  
  height: 300px;
  position: relative;
  overflow: hidden;

  svg {
      background: #1a1a1a;
    border-radius: 12px;
  }
`;
